export const LOAD_DEP_KEYS = {
    session: 'app:session:current' as `${string}:${string}`,
    categories: 'app:categories:expense' as `${string}:${string}`,
    appSettings: 'app:settings' as `${string}:${string}`,
    expenses: `app:expenses` as `${string}:${string}`,
}